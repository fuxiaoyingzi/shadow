<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="15dp" />
    <stroke
        android:width="1dp"
        android:color="@color/colorAccent"
        android:dashWidth="5dp"
        android:dashGap="3dp" />
    <gradient
        android:angle="45"
        android:centerColor="@color/white"
        android:endColor="@color/albumColorPrimary"
        android:gradientRadius="@dimen/album_dp_50"
        android:startColor="@color/colorAccent"
        android:type="linear"
        android:useLevel="true" />

    <!--
    android:angle
    渐变的角度。必须是 45 的倍数，默认值为 0。0 为从左到右，90 为从上到下
    android:centerX
    渐变中心的相对 X 轴位置 (0 - 1.0)
    android:centerY
    渐变中心的相对 Y 轴位置 (0 - 1.0)
    android:startColor
    渐变的起始颜色
    android:centerColor
    渐变的中间颜色
    android:endColor
    渐变的结束颜色
    android:gradientRadius
    渐变的半径，仅在 android:type="radial" 时适用
    android:useLevel
    一般为 false，否则可能达不到预期显示效果，除非把它当作 LevelListDrawable 来使用
    android:type
    渐变类别。它的值可以为：linear(线性)，默认值、radial（径内渐变）和sweep（扫描渐变）
    -->

</shape>